// We define a function that takes one parameter named $.
(function ($) {
    jQuery(document).ready(function ($) {
        if ($('.owl-carousel-header').size()) {
            $('.view-header-slideshow').owlCarousel({
                items: 1,
                dots: true,
                loop: true,
            });
        }
    });
}(jQuery));